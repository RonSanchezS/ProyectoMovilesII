import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'practico2Pokedex',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
